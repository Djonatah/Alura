spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/medvollapi
    username: root
    password:
  jpa:
    show-sql: false
    properties:
      hibernate:
          format_sql: false

server:
  error:
    include-stacktrace: never

medvollapi:
  security:
    token:
      secret: ${JWT_SECRET:MySecret123}